name: Hello Actions

on:
  push:      
    branches: [ "main" ]

jobs:
  hello-job:
    runs-on: ubuntu-latest  
    
    steps:
      - uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello GitHub Actions!!"
